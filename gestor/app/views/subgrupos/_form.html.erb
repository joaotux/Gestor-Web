<%= simple_form_for(@subgrupo, html: {class: 'form-vertical'}) do |f| %>
  <%= f.error_notification %>

    <%= f.input :nome, placeholder: 'Marca' %>
    <%= f.association :grupo %>
    Inativo
    <%= f.check_box :inativo %><br><br>

    <%= f.button :submit %>
<% end %>
